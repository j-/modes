import * as React from 'react';
import Mode from '../containers/Mode';

const App: React.StatelessComponent = () => (
	<div className="App">
		<h1>Mode</h1>
		<Mode />
	</div>
);

export default App;
